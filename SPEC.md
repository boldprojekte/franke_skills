Add a top-level SKILLS.md to the repository root. It must list every skill found under skills/ (each folder containing a SKILL.md), grouped by its parent category folder, as a markdown table with two columns: skill name (linked to its SKILL.md) and the one-line description taken from the description field of that SKILL.md YAML frontmatter (truncate to 140 chars at a word boundary if longer). Start the file with a one-paragraph note that it is maintained manually for now and how to update it. Do not modify any other file.
