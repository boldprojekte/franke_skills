Add a small stdlib-only Python script scripts/list_skills.py to this repository. It scans skills/ recursively for SKILL.md files, parses the name and description fields from each YAML frontmatter without any third-party dependency, and prints a JSON array of {category, name, description} objects to stdout (category = the folder directly under skills/). Include a --markdown flag that instead prints the same data as a markdown table. Add a short Usage section for the script at the end of the repository README.md. Do not modify anything else.
